{
  "name": "conceitos-js-lc",
  "version": "1.0.0",
  "main": "index.js",
  "author": "ThaSMorato <tha.s.morato@gmail.com>",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "npx nodemon --ignore database.json --exec node --experimental-json-modules src/index.js",
    "test": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --runInBand",
    "test:watch": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --watch --runInBand",
    "test:cov": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --no-cache --runInBand --coverage"
  },
  "dependencies": {
    "chai": "^4.3.4",
    "chalk": "^4.1.2",
    "chalk-table": "^1.0.2",
    "draftlog": "^1.0.13"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^27.2.2",
    "nodemon": "^2.0.13"
  }
}
